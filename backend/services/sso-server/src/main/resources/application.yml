server:
  servlet:
    context-path: /sso

spring:
  application:
    name: sso-server
  profiles:
    active: dev

user:
  service:
    url: http://localhost:8081/user

sso:
  ticket:
    expiration: 86400000
