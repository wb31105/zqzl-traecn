server:
  port: 8081

spring:
  profiles:
    active: local
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8081/oauth2/jwks

grpc:
  server:
    port: 9091
  client:
    user-server:
      address: static://localhost:9092

sso:
  oauth2:
    issuer-uri: http://api.local.bw.com
  web:
    login-url: http://sso.local.bw.com/login
    logout-success-url: http://sso.local.bw.com/login?logout
  client:
    user-web-redirect-uri: http://admin.local.bw.com/sso/callback

logging:
  level:
    com.sso: DEBUG
